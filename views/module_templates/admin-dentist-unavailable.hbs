{{#each sched}}
    <tr>
        <td style="font-weight: bold;">{{time}}</td>
        <td class="right aligned">
            <i class="large trash icon link delete-unavailable" data-id="{{_id}}" data-time="{{time}}" id="delete-unavailable-button-{{@index}}"></i>
        </td>
    </tr>
{{/each}}